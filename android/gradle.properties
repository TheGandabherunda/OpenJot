# Your existing Gradle settings
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.workers.max=1
android.experimental.enableSourceSetManifestMerging=true

# START: ADDED FOR JVM COMPATIBILITY
# This will suppress the inconsistent JVM target validation error,
# which is often necessary when dependencies use different Kotlin versions.
kotlin.jvm.target.validation.mode=ignore
# END: ADDED FOR JVM COMPATIBILITY
